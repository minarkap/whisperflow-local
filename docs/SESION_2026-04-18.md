# Sesión 2026-04-18 — Resumen y cierre

## Problemas resueltos

### 1. Alucinaciones de Whisper ("Plug Plug Plug...", "costellas a las costellas...")
**Causa raíz:** `condition_on_previous_text=True` (por defecto) hacía que Whisper se alimentara de su propio output y entrara en loop.

**Solución multicapa:**
- `condition_on_previous_text=False` + `temperature=0.0` → elimina la causa raíz
- `_filter_segments`: `break` al primer segmento con alucinación (no `continue`)
- `_truncate_loop`: detecta repetición de n-gramas al final del texto ensamblado y trunca ahí
- `_is_hallucination`: detección de palabra única dominante o frase corta repetida
- Watchdog: umbral mínimo de 1.5s (vs 1.0s normal) para grabaciones accidentales

### 2. App atascada / daemon congelado
**Causa raíz:** Whisper colgado no liberaba el lock → siguiente llamada bloqueaba indefinidamente.

**Solución:**
- `lock.acquire(blocking=False)` (trylock): descarta nueva transcripción si la anterior sigue viva
- Timeout wrapper: `max_wait = secs * 8 + 6` → hilo de transcripción con `t.join(timeout)` → si sigue vivo, libera estado y continúa

### 3. Latencia en inicio de grabación y beep
**Causa:** beep usaba `subprocess + afplay` (WAV en disco), y `get_active_app()` bloqueaba el hilo principal.

**Solución:**
- Beep pre-generado en numpy + `sounddevice.play()` → instantáneo
- Orden en `on_press()`: `recorder.start()` primero → beep → `get_active_app()` en hilo background
- `time.sleep(0.4)` → `0.15` en paster.py tras Cmd+V

### 4. Race condition en `recorder.stop()`
**Causa:** `queue.empty()` + `get_nowait()` no es atómico.
**Solución:** Loop `try/except` con `get_nowait()` hasta `Empty`.

### 5. O(n²) en `_truncate_loop`
**Causa:** `next(i for i, g in enumerate(ngrams) if g == ng)` re-escaneaba toda la lista cada vez.
**Solución:** Dict `first_seen[ng] = i` durante la primera pasada → O(n).

## Optimizaciones adicionales
- Todas las regex de `formatter.py` pre-compiladas al importar el módulo
- `mlx_whisper` guardado como `self._mlx_whisper` en `load()` (no re-importado en cada llamada)
- `sounddevice` importado a nivel de módulo como `_sd`
- CoreGraphics cargado con `try/except` + flag `_CG_AVAILABLE`

## Commits de esta sesión
```
e0b53c4 Optimizaciones finales: O(n) en truncate_loop, regexes compilados, imports limpios
46cdcdd Corregir bloqueo de lock y filtrar ruido del watchdog
4741b3f Desbloquear estado con timeout y acelerar Whisper con temperature=0
35809ce Eliminar latencia de inicio de grabación y pegado
1a62c94 Revisión completa: robustez, eficiencia y errores corregidos
9571353 Detectar releases perdidos consultando el estado físico del hardware
```

## Estado final
El daemon está corriendo y se ha verificado con `tail -f ~/Library/Logs/whisperflow-local.log`. Todos los problemas reportados han sido resueltos.

## Posibles siguientes pasos (si se retoma)

| Prioridad | Mejora | Por qué |
|---|---|---|
| Media | **Indicador visual** (barra de menú) que muestre IDLE/RECORDING/TRANSCRIBING | El usuario no sabe en qué estado está el daemon sin mirar logs |
| Media | **Modo push-to-talk con timeout automático** (ej: 60s máx) | Evita grabaciones infinitas si el OS pierde el release y el watchdog falla |
| Baja | **Soporte multi-idioma dinámico** (detectar idioma automáticamente) | Actualmente se configura en `config.toml`; `language=None` ya lo activa pero es lento |
| Baja | **Estadísticas de uso** (latencia media, nº de alucinaciones descartadas) | Útil para afinar los umbrales de detección |
| Baja | **Test suite mínima** para `transcriber.py` y `formatter.py` | Los umbrales de detección de alucinaciones son frágiles sin tests |
